# Application
spring.application.name=solar-match-api
server.port=8080

# Database - Local Development
spring.datasource.url=jdbc:postgresql://localhost:5432/solarconnect
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Logging
logging.level.com.solarconnect=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# CORS (allow React client)
cors.allowed-origins=http://localhost:3000,http://localhost:5173


# Prod Settings
#
## Database - will be injected from environment
#spring.datasource.url=${DATABASE_URL}
#spring.datasource.username=${DATABASE_USERNAME:postgres}
#spring.datasource.password=${DATABASE_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver
#
## JPA Configuration
#spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.show-sql=false
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
#
## Logging
#logging.level.com.solarconnect=INFO
#logging.level.org.springframework.web=INFO
#
## CORS - set your production frontend URL
#cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://your-frontend.nhost.app}